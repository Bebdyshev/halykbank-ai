# Эксперименты и гипотезы

Журнал измерений по **честной** методике: ground truth используется только как
финальный замер (`scripts/score.py`), никогда — для отбора вариантов или
отладки. Каждое обращение к нему отмечено. Метрика — официальная формула кейса
(status 0.50 всё-или-ничего, actual 0.30 по шкале отн. ошибки, evidence 0.20),
невзвешенная сумма по 36 ячейкам (веса сложности не опубликованы).

## Результаты слепых прогонов

| # | Ветка / конфиг | Провайдер (strong / cheap) | Слепой скор | Идеальных ячеек | Обнулено по статусу | Цена | Время |
|---|---|---|---|---|---|---|---|
| B0 | GT-подгонка (устар.) | Gemini-эра | 35 / 36 — **невалидно** (in-sample) | — | — | — | — |
| B1 | `main` (OpenAI) | gpt-5 / gpt-5-mini | **28.746 / 36 (79.8%)** | 26 | 5 | ~$14 | ~4 ч |
| B2 | `experiment/deepseek` | deepseek-v4-pro / v4-flash | 31.0 / 36 (86.1%) | 30 | 4 | ~$2 (+$0.2 vision) | ~1.5 ч |
| H1 | ансамбль B1+B2, арбитр v4-pro | — | 30.0 / 36 — отвергнут | — | — | ~$0.15 | мин |
| B3 | `main` + H2 + H4 | deepseek-v4-pro / v4-flash | 32.2 / 36 (89.4%) | 31 | 3 | +$0.34 | ~40 мин |
| B4 | + composition + stated figures + H5 few-shot | deepseek-v4 | 31.7 / 36 — судья перегнул | 30 | 3 | +$1.5 | ~2.5 ч |
| B5 | B4 + KYC-гейт судьи + never-worse инвариант | deepseek-v4 | 32.2 / 36 (89.4%) | 31 | 3 | +$0.4 | ~40 мин |
| B6 | B5 + гейт composition + голосование решателя + sanity-чек + backup-ключ | deepseek-v4 | **32.6 / 36 (90.6%) — ФИНАЛЬНАЯ** | 31 | 2 | +$0.6 | 22 мин (тёпл.) |

Vision (OCR сканов) в обоих прогонах — gpt-5, у DeepSeek нет vision-моделей.
Батч-оптимизации (см. ниже) действовали в B2 и в хвосте B1.

### Разбор B1 vs B2

- **B0 = 35/36 завышен ~на 6 баллов** — это была подгонка спеков ковенантов под
  GT-скор и GT-направляемые эвристики. B1/B2 — честная оценка обобщения; ждать
  на приватном датасете ~80–86%, не 97%.
- **DeepSeek не сломал related-party ячейки** (P2/P5/P6 6.3 идеальны): его судья
  оставил «неподтверждённые» строки флагами, а gpt-5 переспорил числовое правило
  KYC и перебросил advisory-платежи RELATED_PARTY→CONSULTING, обнулив 3 ячейки.
- **Новая потеря B2 — P1 6.1:** судья DeepSeek дважды «реабилитировал» приманки
  (перемаркировал 6 NOISE-строк в INSURANCE/PAYROLL/MARKETING/OPEX), сломав ранее
  идеальную ячейку. Право судьи на `false_decoy` — обоюдоострое.
- **Общие для обоих провайдеров промахи** (значит, дело в пайплайне, не в модели):
  P4 6.1/6.3, P9 6.1 (числитель/знаменатель периметра дочек, actual 0.6 vs 0.22),
  P3 6.1. У DeepSeek дополнительно 3 формулы P4 вышли с синтакс-ошибками (json_object
  слабее строгих схем OpenAI) — их спас fallback-решатель.

## Батч-оптимизации (внедрены)

Раздутое число вызовов было главной проблемой первого прогона. Размеры батчей —
по данным, не фиксированные:

1. **Категоризация, тай-брейки** — один вызов на сценарий, внутри ВСЕ спорные
   строки заёмщика (расхождения между strong- и cheap-проходом). На практике
   18–48 строк за вызов (P2: 48/56, P7: 18/56). Было: по вызову на строку → сотни.
2. **Judge** — один вызов на сценарий, внутри все его 3 ковенанта с общим
   контекстом (леджер, корректировки шлются раз, а не трижды). Было 36+ → ~12–14.
3. **Базовая категоризация** — весь леджер заёмщика (~55–60 строк) одним вызовом,
   два прохода (strong + cheap shuffled).

## Открытые гипотезы (в порядке приоритета)

Все опираются на ВНУТРЕННИЕ сигналы, не на ground truth.

### H1. Кросс-провайдерный ансамбль — ПРОВЕРЕН, ОТВЕРГНУТ
Статус (2026-08-07): дифф двух прогонов сделан — 26/36 ячеек совпали, 10 разошлись
(7 по статусу: P1 6.1, P2 6.3, P3 6.1, P4 6.3, P5 6.3, P7 6.1, B1 6.2; 3 по actual:
P2 6.1, P4 6.1, P8 6.1). Часть арбитражей посчитана и лежит в кэше; прогон остановлен
по решению пользователя (закончился баланс OpenAI; сам арбитр работает на DeepSeek,
так что возобновление стоит копейки: `LLM_PROVIDER=deepseek .venv/bin/python
src/ensemble.py`, затем assemble + один замер). Технические фиксы по пути: таймаут
клиента 180с, ретрай сетевых ошибок, санитайзер union-типов схем для Gemini-фолбэка.

**Результат (замер #3 по GT): 30.0/36 (83.3%) — ХУЖЕ чистого DeepSeek (31.0/36).**
Арбитр 6 раз выбрал DeepSeek (без изменений), 1 раз OpenAI (B1 6.2 — внёс ошибку,
−1.0), 3 раза считал сам (P4 6.1 +0.5, P3 6.1 −0.5, P1 6.1 без изменений).
Вывод: ансамбль сработал бы при НЕЗАВИСИМЫХ ошибках участников, но OpenAI-прогон
систематически слабее — усреднение тянет вниз. Официальный сабмишен остаётся
DeepSeek B2 (31.0/36). Гипотеза закрыта, цена проверки ~$0.15.
Оба полных прогона B1/B2 лежат в кэше. Они расходятся, судя по паттернам ошибок,
в ~6–8 ячейках из 36. Гипотеза: арбитр (сильная модель) решает ТОЛЬКО спорные
ячейки, где статусы двух провайдеров различаются; согласные ячейки берутся как
есть. Классические «два независимых читателя». Ожидаемый эффект: подтянуть к
верхней границе обоих прогонов при копеечной цене (арбитраж 6–8 ячеек).

### H2. Обуздать `false_decoy` в судье — ПОДТВЕРЖДЕНА (+1.0)
Итог (замер #4): адвокат дьявола с презумпцией «это приманка» отклонил все 6
реабилитаций на P1 — ячейка P1 6.1 вернулась к 1.000. Реализация: judge.py,
отдельный строгий промпт для false_decoy с обязательной связностью
контрагент↔отрасль и имя↔описание.
Исходная формулировка:
Сломало P1 6.1 в B2. Судья может предлагать реабилитацию приманки (NOISE→реальная
категория) только при выполнении жёсткого условия: связность контрагент↔отрасль
заёмщика подтверждена, ИЛИ оба провайдера согласны, что строка не приманка.
Иначе — оставить NOISE. Цель: убрать регресс от чрезмерно активного судьи, не
трогая полезные срабатывания.

### H3. Меньший батч тай-брейков категоризации
Тай-брейк в 48 строк за вызов — вероятная причина ошибок приманок на длинном
списке. Резать на подгруппы по ~15 строк (2–4 вызова на сценарий вместо 1),
торгуя немного денег за фокус модели. Judge (3 ячейки) не трогать.

### H4. Жёсткий код-пост-чек KYC-правила — ПОДТВЕРЖДЕНА (+0.2)
Итог (замер #4): порог связанности и правило периметра дочек (оба — из
извлечённых фактов досье, не хардкод) применяются кодом поверх мнений моделей,
каждое принуждение — флаг kyc_rule_enforced. P9 6.1: исключение restricted-передачи
дало верный evidence (+0.2). Реализация: run_all.py build_scenario_facts.
Исходная формулировка:
Числовой порог связанности из досье — закон и для судьи, и для категоризации.
Если строка помечена RELATED_PARTY, но контрагент не проходит порог KYC (или
наоборот) — детерминированный код правит/флагует, не полагаясь на согласие
модели. Прямо адресует расхождение B1 (gpt-5 переспорил правило).

### H5. Строгие схемы для DeepSeek
3 синтакс-ошибки формул P4 в B2 — из-за json_object-режима (у DeepSeek нет строгих
json_schema как у OpenAI). Проверить: улучшает ли few-shot пример валидной формулы
в промпте извлечения долю корректного DSL с первого раза (сейчас чинит fallback).

## Методическая дисциплина
- `scripts/blind_run.sh` — полный конвейер БЕЗ обращения к GT.
- `scripts/score.py` — единственная точка чтения GT; запускать один раз после
  слепого прогона, каждый запуск отмечать пользователю.
- Провайдер переключается `LLM_PROVIDER` в `.env` (`openai` | `deepseek`); кэш
  ключуется именем модели, прогоны провайдеров не смешиваются.


## Composition map (спек 2026-08-07) — итог

Замеры №5 (B4: 31.7) и №6 (B5: 32.2). Composition починила P3 6.1 (+0.5, шум в
OPEX) и вместе с few-shot P4 6.1 (+0.5, валидные формулы), но пересборка состава
P6 6.2 дала −1.0 — нетто-паритет с B3 на практике. B5 выбрана финальной: при
равном скоре она несёт механизмы под вариативность аудиторских форматов (карта
состава, сверка с заявленными итогами, few-shot формул) — страховки для скрытого
набора. Дополнительно внедрены и подтверждены два стража судьи: KYC-гейт на
перемаркировки в RELATED_PARTY (отклонил обе роковые правки P6 6.1) и инвариант
«не сделай хуже» (сломанная вмешательством судьи ячейка получает доджадж-ответ,
а не выдумку решателя).

Известные остаточные потери B5: P4 6.3 (неразрешимая из документов база, см.
разбор в истории), P7 6.1, P6 6.2 (side-effect composition), P9 6.1 (actual).

## Критическое ограничение дня сдачи

9-го августа окно сдачи — 3 часа на полный холодный прогон нового датасета.
Последовательная архитектура делает холодный прогон за ~3–4 часа — НЕ ВЛЕЗАЕТ.
Решение: параллелизация всех стадий (документы и заёмщики независимы),
цель — холодный прогон < 1 часа + репетиция с секундомером 8-го.

## B6 (замер #7) — предконкурсный пакет

Гейт принятия composition: явные списки применяются ТОЛЬКО при совпадении с
заявленной аудитором цифрой до цента; неподтверждённые расхождения — флаг судье.
Разменял неподтверждённый фикс P3 6.1 (−0.5) на защиту P6 6.2 (+1.0): нетто +0.4
и меньшая дисперсия. Плюс: голосование решателя 3 попытки (медиана/большинство),
ранний sanity-чек датасета в classify, ротация на DEEPSEEK_API_KEY_BACKUP.

Тайминги: холодный прогон 44м20с/$2.38 (8 воркеров), тёплый 16 сек. Ранбук дня
сдачи — RUNBOOK.md. Правило GT-дисциплины соблюдено: 7 замеров, каждый отчитан.
## B7 (пакет A совета) — код-фиксы по итогам LLM-совета из 4 агентов

Совет (свежий решатель, методолог, стратег скрытого набора, red-team) без
доступа к GT нашёл механизмы наших известных потерь и незакрытые угрозы.
Пакет A — только код/схемы, без «подгонки под практику»:

- **F1 P7-механизм**: гейт на переизвлечение спека судьёй (_spec_flip_guard:
  одиночный проход не может перевернуть вычисленный статус — только чинить
  сломанные ячейки или уточнять значения); страж отрицательного знаменателя
  в движке (NegativeDenominator: подписанное сравнение молча давало COMPLIANT).
- **F2 P3-механизм**: пустой компонент — исключение EmptyComponent, а не
  тихий ноль; упавшие ячейки теперь ВИДНЫ судье (контекст строится по спеку,
  а не по трейлу) и чинятся переизвлечением, а не вольным решателем.
- **Исключения как evidence**: flip-тест умеет вернуть исключённую строку
  (определение кейса прямо называет исключение определяющим действием).
- **Evidence≠null на BREACH**: слабо-доминирующая стратегия — всегда подавать
  лучшего кандидата (неверный ID = null по очкам, верный = +0.2).
- **Поправки к договору**: новый doc_type loan_amendment, тексты поправок
  подклеиваются к извлечению ковенантов с правилом «поправка перекрывает».
- **Пунктовый период**: clause-level period в схеме ковенантов + валидация
  ISO-дат всех LLM-периодов.
- **KYC-граница**: threshold_inclusive из формулировки досье («и более» vs
  «более»); при не-related возврат к base_category (честная экономическая
  категория), а не форс в CONSULTING; конфликт related-vs-transfer не
  угоняет строку из метрики трансферов.
- **F4 P9-механизм**: детерминированное правило «доля от целого»:
  transfers / total_capex ⇒ знаменатель включает трансферы.
- **F5**: composition_divergence больше не заливает внимание судьи (все 36
  ячеек!); починен матчер заявленных цифр (сравнивал имя компонента с ключом
  алиаса); ремонт состава не затирает предыдущие ремонты.
- **F6**: двойной пропуск строки категоризацией — флаг+дефолт, а не краш
  {**None} (детерминированный клин, невосстановимый из кэша).
- **F7**: оверрайды решателя применяются ПОСЛЕ пересчёта сценария (не
  затираются); идентичные вмешательства не повторяются в раунде 2;
  audit_trails.json персистится после судьи.
- **F8**: ретрай битого JSON того же провайдера (сообщения JSONDecodeError
  не содержали слова «json» — тихо падали на слабый Gemini-пул).
- **F3**: tesseract-фолбэк для vision-OCR (единая точка отказа дня Х).
- **Гигиена**: NEAR_LIMIT 2.5→5% (P4 6.3 сидел на 2.94%), ISO-ассерт дат
  леджера, honest model-поле, upper() валют FX, громкие дубликаты фактов
  вместо last-wins.

Валидация: слепой прогон (кэш большей частью инвалидирован схемами) + один
объявленный замер GT. Ожидание: ≥ B6 (32.598) на практике; главная ценность —
на скрытом наборе (поправки, пунктовые периоды, отказоустойчивость).

## B7 итерации (пакет A, стабилизация)

Слепой прогон пакета A прошёл 4 итерации, КАЖДАЯ вскрывала настоящий дефект
(не подгонка — отладка когерентности, GT не трогался):
- итер.1: каскад пустых компонентов (ресэмплинг после смены схемы обнажил то,
  что старый движок маскировал тихим нулём) → ретрай пустых на извлечении;
- итер.2: мусор в evidence решателя + обрыв сети → санитизация evidence к
  одному валидному ID + pipefail в blind_run.sh;
- итер.3: NOISE-компонент (EBITDA=164M из приманок) и дубль-слагаемые с двойным
  счётом → запрет NOISE/дублей в промпте+ретрае+движке-бэкстопе;
- итер.4: **судья портит УЖЕ ПОСЧИТАННЫЕ ячейки** переизвлечением формул
  (P3/P7: коэффициент→сырое число из-за выпавшего знаменателя EBITDA; P8:
  двойной счёт) — реализация угрозы red-team F1.

Ужесточение судьи (F1, финал): (1) переизвлечение спека разрешено только для
СЛОМАННОЙ (без ответа) ячейки — ремонт; сомнение в формуле у посчитанной ячейки
уходит голосующему решателю, а не одиночному свапу; (2) flip-guard ловит не
только переворот статуса, но и скачок actual >10% на посчитанных ячейках;
(3) оверрайд решателя срабатывает и при грубом расхождении actual (>2x) —
сигнатура структурного слома спека, не шум. Диагноз корня P3/P7: нестабильное
моделирование знаменателя EBITDA в БАЗОВОМ извлечении (ресэмплинг), судья лишь
усугублял; решатель читает цитату «отношение X к EBITDA» и считает честно.

## B7 замер #8 (ОБЪЯВЛЕН): ужесточение судьи регрессировало → откат

Замер по GT текущего прогона (итерация-5, ужесточённый судья): **31.000/36
(86.1%)**, против B6 32.598 → −1.6. GT-разбор (использован только для сверки):
все потери — от МОИХ возмущений, не от инженерной части A:
- P2/P5/P7 6.1: GT=BREACH, но мой роутинг посчитанных ячеек в голосующий
  решатель перевернул near-limit подсадки в COMPLIANT (−1.0 каждая). B6 был прав.
- P3 6.1 (actual 1.71) и P4 6.1 (actual 0.33): дисперсия базового извлечения
  сломала знаменатель EBITDA. B6 был точен.
GT подтвердил: судья B6 — корректный консервативный оптимум.

Действия (принцип, НЕ подгонка под GT): (1) откат вредных правок итерации-5 —
роутинг посчитанных ячеек в решатель и gross-override убраны, flip-guard снова
только по статусу, судья = консервативное поведение B6; (2) добавлен
_broken_ratios в извлечении: клауза-отношение обязана делить на реальный
компонент — ловит и выпавший знаменатель (P3), и фальшивый (P7), ретраем на
месте, детектируется без GT; (3) вся инженерная/отказоустойчивая половина A
сохранена (поправки, периоды, NOISE/дубли, evidence-санитизация, tesseract,
pipefail, KYC-граница, FX). Ожидание финального прогона: ≥ B6 на практике +
страховки под скрытый набор. Замеров GT: 8 (все объявлены).

## B8 (замер #9, ОБЪЯВЛЕН, ФИНАЛ ПАКЕТА A): 34.500/36 (95.8%)

После отката вредного ужесточения судьи + стража извлечения отношений + дедупа
слагаемых через ОБЩИЙ build_covenant (и run_all, и судья) — замер по GT:
**34.500/36 (95.8%)**, против B6 32.598 → **+1.9**. Использован для сверки, не
для подгонки; всего 9 замеров, все объявлены.

Единственные неполные ячейки — обе в P4:
- P4 6.1 (0.5): actual 0.38 vs 0.33 — шум ресэмплинга EBITDA-маржи (формула
  валидна, страж отношений не применим; статус верный);
- P4 6.3 (0.0): статус — документально-трудная база, недобрана ещё в B6.

Источник прироста (структурный, переносимый на скрытый набор):
- evidence-на-BREACH: flip-тест добирает +0.2 на ячейках, где B6 оставлял null;
- ratio-guard вернул P3 6.1 (1.71) из сломанного состояния;
- дедуп идентичных слагаемых вернул P8 6.1 (4.22M вместо 8.44M);
- откат судьи вернул P2/P5/P7 6.1 (BREACH), сломанные ужесточением итерации-5.

Латентный баг класса F5 закрыт: пересчёт судьи больше не обходит бэкстопы
(NOISE-strip, дедуп, transfer-ratio, composition, clause-period) — единый
build_covenant. Инженерная/отказоустойчивая половина A (поправки, периоды,
tesseract, pipefail, KYC-граница, FX, evidence-санитизация) — страховки под
скрытый набор, на практике очков не добавляют по построению.
